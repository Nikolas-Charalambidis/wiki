# The ROOT is a special unnamed module name that is generated to the /build/site/ directory instead of a named one
name: ROOT
title: Wiki
# The tilda (~) means the component is unversioned
version: ~
start_page: ROOT:ROOT.adoc
nav:
  - modules/ROOT/nav.adoc